1. Laravel

```bash
laravel new project-name

# with PEST

composer required --dev rector/rector driftingly/rector-laravel

# composer.json
"format": [
    "vendor/bin/pint",
    "vendor/bin/rector"
],
```
